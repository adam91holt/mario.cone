MARIO.CONE build loop — hourly. Repo adam91holt/mario.cone (PUBLIC), branch claude/waves-phases-routine-setup-faggxq. WFDIR=/root/.claude/projects/-home-user-mario-cone/796d0d90-4635-56a8-a763-6958ed7d5f0d/subagents/workflows

*** THE BUILD IS CLOSED. THE USER ASKED FOR THIS ON 2026-08-13, IN THESE WORDS: "after this round, can we do the last pr and pause any progress for now". ***
LAUNCH NO WAVES. Not a wave 6, not a coherence pass, not a phone critic, not "one more round while we wait". Everything below this block — the remaining-work list, the run ids, the resume procedure — is now A RECORD OF WHERE IT WAS LEFT, not a queue to pull from. It is written down so that whoever picks this up, whenever the user asks, does not have to re-derive six days of findings.
The close-out was: wave 5's round finished, the gates were run, the board was brought up to date, the branch was merged to main, and this trigger was disabled.
Restarting is the user's call and theirs alone, in their own words. Do not propose it, do not schedule it, and do not treat a green board or an idle container as permission.

FIRST TICK IN A FRESH CONTAINER: `ls node_modules` — if it is missing, `npm install` before anything else. The container is reclaimed on inactivity and comes back with an empty node_modules; every agent that runs typecheck or smoke without it fails on something that has nothing to do with its piece.

AND BEFORE YOU BELIEVE ONE BYTE OF THE LOCAL STATE: `git fetch origin <branch> && git log --oneline HEAD..origin/<branch>`. A RESTORED CONTAINER CAN COME BACK ON A STALE CHECKOUT. On 2026-08-13 the container came back at 5b817e4 with a stale working tree while origin was five commits ahead at dd27eac — and the stale tree was internally consistent, so nothing looked wrong. An entire tick was spent re-deriving work that was already pushed: re-finding the rung-6 review sheet, re-writing `pinRung0` into capture.mjs, re-reading a journal that was frozen at 11 agents when the wave had actually reached 17. THE THINGS THAT LIE ARE ALL OF THEM: the working tree, `git log`, `handoffs/*`, `tools/progress.state.json`, AND `$WFDIR/$RUN/journal.jsonl` — the workflow journal is under /root/.claude, is not in git, and freezes at whatever the snapshot caught. THE ONLY DURABLE RECORD IS THE PUSHED HISTORY. Read `git log origin/<branch>` and the commit MESSAGES; this project writes its findings into them precisely so a lost container cannot take a verdict with it.
NEVER RESUME A WAVE OFF A STALE JOURNAL. Resume reads that file as its cache; a journal frozen at 8 results when the run reached 13 does not skip the missing five, it RE-BUYS them. If the journal's count disagrees with what the pushed commit messages say the wave reached, the wave is not resumable from this container — close it out on the verdicts the history records and launch the next one.

EVERY TICK, before anything else: `node tools/session.mjs`. It refreshes docs/session/prompts.md and docs/session/session.jsonl — the conversation that is building this game, which the user asked to be published and kept current. It exits non-zero if a message body was truncated; if that happens fix the tool rather than committing a mangled archive. Commit the refresh with whatever else the tick produces.
It MERGES every transcript in the project dir with the archive already in the repo, keyed on record uuid, and is append-only and idempotent — safe to run from any session. It used to open one hardcoded transcript and broke the moment the build changed hands. Never "fix" it by pointing it at the current session and writing the result: finished sessions' transcripts are deleted from disk long before the work they describe, and docs/session/session.jsonl is the only surviving copy of the first six days. If a run reports FEWER records than the file already had, stop and find out why rather than committing it.

Do NOT trust any run id written here — resolve it: RUN=$(ls -t $WFDIR | head -1). WAVE 5 = wf_4b71b881-5e9, launched 2026-08-13 08:31 UTC: look + camera, two rounds, no carry, tools/wave1.workflow.mjs. The first wave ever judged off a rung-0 review sheet, and the first time either piece has been judged at all. WAVE 4 (courses+perf, wf_6b3a5457-38c) IS CLOSED. It reached 17 agents started and 13 returned before the 2026-08-13 container churn, and its journal is no longer recoverable on any live container, so it must not be resumed. What it earned, from the pushed history: courses 6.5 then 7.0, perf 6.5 twice, every blind A/B still Mario Kart. Wave 3 (wf_d54eb1d2-a32) landed all eight agents with seven verdicts, both pieces at 7.0, npm test green on all six gates, and the countdown WARN from the original handoff finally gone.

ITS ARGS ARE ON DISK: handoffs/wave.args.json. That file is `{script, launched, note, args}` — the SCRIPT PATH IS IN IT because the project now has three wave scripts and a resume against the wrong one is a silent relaunch. Resume passes the args byte-for-byte: `Workflow({scriptPath:<the file's "script">, resumeFromRunId:RUN, args:<the file's "args" object, exactly>})`. Note it is the `args` FIELD, not the whole file. Do not retype from memory; args that differ by one character miss the cache and re-buy the whole wave. When you launch a NEW wave, rewrite that file in the same breath, and keep `args` slimmed to what buildPrompt actually reads — score, pass, blindPick, biggestGap, directive, evidence — so the file and the launch cannot drift.

STEP 1 — PROVE the wave is alive. Two checks; the second has never been wrong:
  date -u
  ls -lt --time-style=+%H:%M:%S $WFDIR/$RUN/agent-*.jsonl | head -3
  ps -eo etime,comm | grep -w claude
(a) Newest agent-*.jsonl mtime older than ~25 minutes = dead. USE `ls -t`; a plain `ls | tail` sorts ALPHABETICALLY and once hid the only live agent behind eleven finished ones.
(b) ELAPSED of the main `claude` process. DECISIVE. If claude has been alive for LESS time than the gap since the last agent write, the container restarted and every in-flight agent is dead, however recent the mtimes look. Two agents whose last write is the IDENTICAL second is the same signature. Do NOT grep for claude in a `--sort=-pcpu | head -4` list — Chrome outranks it and it will not appear.
Never judge by files existing or Chrome counts: subagents run IN-PROCESS inside the main claude process, so `ps aux | grep claude` shows nothing for a healthy wave.

Count: node -e "const fs=require('fs');let s=0,r=0;for(const l of fs.readFileSync(process.argv[1],'utf8').trim().split('\n')){try{const e=JSON.parse(l);if(e.type==='started')s++;if(e.type==='result')r++;}catch{}}console.log(s,r)" $WFDIR/$RUN/journal.jsonl
Concurrency is 2 on this 4-core box.

STEP 2 — If DEAD, RESUME, DO NOT RELAUNCH:
  Workflow({scriptPath:"<the same script>", resumeFromRunId:"<the dead RUN>", args:<the SAME args object, byte-for-byte — read handoffs/wave.args.json>})
Completed agent() calls return from cache instantly and only the killed agents re-run, carry intact. Args must match exactly or the cache misses. First `npx tsc --noEmit`, then commit and push whatever exists (say in the message if unverified). Read the dead run's journal for results that already have a `score` — those verdicts are earned and must never be re-bought:
  node -e "const fs=require('fs');for(const l of fs.readFileSync(process.argv[1],'utf8').trim().split('\n')){try{const e=JSON.parse(l);if(e.type==='result'&&e.result?.score!==undefined)console.log(e.agentId,e.result.score,e.result.biggestGap);}catch{}}" $WFDIR/$RUN/journal.jsonl
IF TYPECHECK FAILS INSIDE A FILE A DEAD AGENT WAS MID-WRITE ON, that is NOT transient — the agent is not coming back to finish it, and the resumed agent restarts that step from scratch. `git checkout --` those files rather than committing a build that cannot compile.
AFTER ANY LAUNCH OR RESUME, VERIFY THE NEW AGENTS ARE THE ONES YOU EXPECTED. A builder's transcript says 'YOUR PIECE:'; a critic's says 'PIECE UNDER REVIEW:'. Also grep 'Observed:' to confirm the carry arrived.
A RESUME DOES NOT REPLAY IN THE ORDER YOU EXPECT, AND THIS IS NOT YET UNDERSTOOD. After the 2026-08-12 resume of wf_108abd86-700, whose four round-2 agents had died on the weekly limit, the agents came back in this order: perf CRITIC at 12:42 returning 7.0, courses CRITIC at 12:50, and a perf BUILDER at 12:58 — a builder starting sixteen minutes after the critic that was meant to review its work. An earlier version of this note claimed the errored builds had replayed as null and been skipped; the builder that started afterwards makes that unsafe to assert, so it is written down as an observation rather than as a mechanism. What is certain: a critic ran before the builder it reviews, so at least one verdict in that run graded the previous round's tree.
THE CHECK THAT MATTERS, WHICH COST NOTHING TO KEEP: after any launch or resume, look at what the new agents ACTUALLY are before believing the wave is doing what you asked. A builder's transcript says 'YOUR PIECE:'; a critic's says 'PIECE UNDER REVIEW:'. If the resumed agents are critics when you expected builders, the round is judging old work — let them finish if the verdict is worth having, then LAUNCH A FRESH WAVE carrying those verdicts rather than resuming again.

STEP 3 — If ALIVE: `npx tsc --noEmit`. If clean, commit and push. Do NOT run captures or smoke while agents are active, and never pkill on any pattern matching "capture.mjs" — agents run the same tools you do.

NEVER MATCH A PROCESS PATTERN THAT YOUR OWN SHELL'S COMMAND LINE ALSO MATCHES. This has now cost time three times, in two forms. `pkill -f "node foo.mjs"` matches the shell running that very command and kills the tick mid-sentence — it looks exactly like a container suspend. And a wait loop written as `until ! pgrep -f "tools/(phone|steer)"; do sleep 5; done` never exits, because the eval'd loop body is itself a process whose command line contains "tools/(phone|steer)". Anchor the pattern (`^node tools/phone`), or resolve the pid once and then `while kill -0 $PID`. To wait on `npm test`, wait on the ROOT pid: the chain is `npm test` -> `sh -c npm run typecheck && ...` -> `npm run test:phone` -> `node tools/phone.mjs`, and after a container restart the root reparents to init but the chain keeps running, so `kill -0 <root>` is the honest signal. Note also that `npm test` under software GL takes upwards of half an hour, most of it in phone.mjs — do not read a slow gate as a hung one.

STEP 4 — If FINISHED:
  a. `npx tsc --noEmit` then `node tools/capture.mjs --smoke`. Typecheck-clean has passed on a build that did not boot; smoke is the real gate. Then `npm test` — typecheck, smoke, countdown, phone, steer, underground.
  b. `node tools/capture.mjs` and LOOK at the PNGs with Read. Never trust an agent's summary.
  c. Update tools/progress.state.json with the real verdicts from the journal, `node tools/progress.mjs`, `node tools/readmeshots.mjs`.
  d. Commit and push. THE MERGE TO MAIN IS THE USER'S CALL RIGHT NOW — a push to main redeploys the live game and the deployed build still has the camera burial in it. Ask before opening the PR; do not merge unprompted. When they say go: PR (base main, draft false), merge, then `git fetch origin main && git reset --hard origin/main && git push --force-with-lease` (merges are squashed, so ff-only will refuse).
  e. Launch the next wave — two pieces, two rounds, carry the verdicts the last wave earned, and write the new args to handoffs/wave.args.json.

PLAYER REPORT — FIXED AND GATED, 2026-08-12. "Sometimes the map is below a layer or something and you kinda go underground and the screen just went brown above the racer and you can't see them."
It was JACKHAMMER QUARRY, and it was layout rather than lens: the quarry benches sat hard against the road with no clearance for a chase rig that trails ~8m behind and ~3m above, so the boom lived inside them. The courses builder set them back. Measured before: 33 of 200 autopilot samples with the lens inside the landscape, worst 3.92m under with the kart ON THE ROAD at 83 m/s. Measured after: 0 of 171, closest approach 3.17m ABOVE the surface, and the two quarry shots the critic reported as a full frame of grey scree are open sky and road again.
`node tools/underground.mjs` IS NOW IN `npm test`. It was deliberately kept out until it had been red — a gate that has never failed is not a gate — and it was red for a day before it went green, which is the only sequence that proves it works. Four acceptance tests now, each written from a real report: countdown, phone, steer, underground.
THREE GUARDS ON THAT TOOL, EACH BOUGHT WITH A WRONG ANSWER. Read them before touching it. (1) Terrain is the two meshes NAMED 'ground' and 'embankment'; picking by vertex count also catches grandstands, crowds and an overhead sign, and "reproduced" the bug 8.5m under on cone-canyon when the camera was passing beneath a gantry under clear sky. (2) reset() takes courseId/vehicleId and SILENTLY IGNORES unknown keys, so passing `course` loads the default — the test measured cone-canyon four times and printed four course names. It now checks snapshot().track.id against what it asked for. (3) The engine's rAF loop never stops and steps the sim by wall time alongside anything the harness drives, so setTimeScale(0) must be re-applied AFTER EVERY reset (reset restores it to 1). capture.mjs has always done this and says why.
STILL UNCHECKED, and the next place to look if it is ever reported again: other vehicles, being spun out, the finish camera, and look-behind on a course whose banks are close.

REMAINING WORK, IN ORDER (handoffs/README.md §5 is the long form):
  1. COURSES — 7.0, 15 rounds, AND STILL DESCENDING. Sameness has now been framed five different ways across five waves — four drawings of one object, then four paint jobs, then four shapes in one world, then two exposures of one place, and now one tarmac under four skies: all four courses are driven on the identical dark-grey speckled asphalt with the identical white markings. Each round the builder closes the layer it was handed and the critic finds the layer beneath it. Unlike perf this is descent, not circling — the OBJECT of the complaint changes every time — and there is a floor to it that is close.
  2. PERF — 6.5, 18 rounds, AND IT HAS PLATEAUED. Three consecutive verdicts land on the same axis: the mid-race ladder is a resolution slider and nothing else (seven rungs shed 3 draw calls out of 311 and 6% of triangles while render scale goes 1.0 to 0.5), and the governor negotiates it in public — inside one sixty-second race the scale walked 1.00 -> 0.85 -> 0.70 -> 0.66 -> 0.51 -> 0.50 -> 0.56, and the FIRST step froze the delivered frame for 3,037ms against a 662ms median while the later steps cost 15.9, 14.6 and 72.3ms. That is pipeline creation, not fill: prime every LADDER scale behind the closed launch board where primeShells already runs, then take the move in one frame instead of rationing it into five chances to hitch. DO NOT KEEP BUYING ROUNDS ON THIS PIECE WITHOUT A NEW AXIS — it has had eighteen, against two for five pieces that have never been judged at all.
  3. CLOSING VERDICTS ON WAVE 1 — THIS IS THE NEXT WAVE, AND IT IS OVERDUE. feel, camera, track, look and cast were merged long ago and have never been judged once; the board still shows them as `review` at 2 rounds each while courses sits at 15 and perf at 18. Every art verdict this build ever produced before 2026-08-13 was made on a rung-6 picture, so `look` in particular has never been seen at full quality by anybody. tools/wave1.workflow.mjs NOW TAKES ARGS ({pieces, rounds, carry}) like wave.workflow.mjs — it used to hardcode all five pieces at three rounds, which is thirty agents in one wave on a box that suspends every 35-90 minutes. Two pieces, two rounds, same as every other wave.
  4. A COHERENCE PASS (tools/coherence.workflow.mjs) for the gaps no single piece owns: the purple tier-3 mini-turbo has still never been seen by anyone; a single carried item is not drawn in the world at all because the orbit rig is only built for count>1; the finish letterbox guillotines the position badge on the one beat it matters. It must NEVER run while a wave is running — whole-repo ownership and per-piece ownership cannot coexist.
  5. A PHONE CRITIC. tools/phone.mjs passes, but no critic has ever played this on glass. The touch layer shipped straight to main off a bug report, unjudged. A critic whose whole brief is the phone build — thumbs on glass, landscape, one-handed steering against MK8 on a Switch handheld — is owed a round.

RENAME TO "GARY'S KART" — PARKED. DO NOT START IT.
The user raised it on 2026-08-11 and then said on the same day: "Just park the rename until I ask for it." So it is NOT triggered by the board going green, NOT part of the definition of done, and NOT something to raise again. It waits for the user to ask for it in their own words. Do not bring it up, do not start it early, do not fold pieces of it into other work. If the board goes fully green and the rename has not been asked for, the build is finished — say so and stop.
The notes below exist only so the work is not re-derived when the user does ask. They are reference, not instructions to act on.
  - It is NOT a sed. `grep -ri mario` returns 115 hits outside docs/session, in four classes:
    1. PRODUCT NAME `MARIO.CONE` / `mario.cone` — 37 hits across 17 files. These are the ones that rename: ARCHITECTURE.md, README.md, docs/how-it-works.md, index.html, package.json, package-lock.json, progress.html, showcase.html, src/render/materials.ts, src/ui/menus/art.ts, src/ui/touch.ts, tools/{coherence,wave,wave1}.workflow.mjs, tools/progress.template.html, tools/session.mjs, tools/showcase.mjs.
    2. `Mario Kart` / `MK8` — ~159 hits. KEEP EVERY ONE. This is the design benchmark the whole critic loop is built on ("write the MK8 reference from memory before looking"). A blind sed on "mario" guts the project's design rationale.
    3. `cone` the MACHINE and `cone-canyon` / `Cone Canyon` the COURSE. KEEP. The player's kart is a road cone and stays one.
    4. FILESYSTEM AND REPO PATHS — `/home/user/mario.cone`, `-home-user-mario-cone`, `github.com/adam91holt/mario.cone`. KEEP unless the repo is genuinely renamed; changing them breaks tools/session.mjs and the workflow scripts.
  - The wordmark is the real work, not the text. `src/ui/menus/art.ts` carries its OWN local `L` table — not ui/glyphs.ts — holding exactly A C E I M N O R. "GARY'S KART" needs G K S T, liftable unchanged from ui/glyphs.ts (same cut of lettering, which is the point of the shared metrics), plus a Y and an apostrophe that exist in neither table and must be drawn in that face: flat terminals, 16-unit key, 100 cap + 13 extension, 6.5 degree slant. It also needs a word space, which the mark has never had — TRACK is letter spacing, not a word gap.
  - The CONE glyph currently stands in for the `.` and the docblock notes it deliberately lands mid-word during the assembly animation. "Gary's Kart" has no dot; the apostrophe slot is the obvious home, but it has to read as punctuation rather than debris.
  - Any art.ts path change requires regenerating the inline copy in index.html — the one duplicated asset in the project and the first frame of the game; the command is in the wordmark docblock. Then re-capture, because the old mark is baked into shots/ and clips/ and into README.md via tools/readmeshots.mjs and tools/showcase.mjs.
  - Do NOT rewrite docs/session/prompts.md or session.jsonl. That is a transcript of what was actually said, regenerated by session.mjs from the real conversation, and the project genuinely was called MARIO.CONE for its whole build.
  - Two things to ask the user at that time, not before: whether to rename the GitHub repo and therefore the Pages URL (github.com redirects a renamed repo; the Pages URL does not, so adam91holt.github.io/mario.cone would 404 for anyone already sent the link); and whether Gary is anyone, since the cast is Foreman, Bollard, Skip, Gravel, Barrier, Tarmac and Detour and contains no Gary.

Keep waves SHORT (two pieces, two rounds) and rely on resume rather than on a wave surviving. Stop when every piece in tools/progress.state.json is "pass". Message the user only when a wave lands, a wave had to be restarted, something needs a decision, or the build is finished.
